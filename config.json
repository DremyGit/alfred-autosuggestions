{
  "request": {
    // suggestion or search API
    "url": "https://api.github.com/search/repositories?q={query}",
    // Support other fetch options
    "headers": {
      // ...
    }
  },

  // path of list
  "input": "items",

  // output templates
  "output": {
    "title": "{{full_name}} - {{description}}",
    "subtitle": "{{language}}, {{stargazers_count}} stars",
    "arg": "{{html_url}}"
  }
}